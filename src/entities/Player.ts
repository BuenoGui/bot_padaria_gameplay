class Player {

    // Substituir NOME por Tell no futuro

    constructor (
        public id_player: string,
        public level: number,
        public xp: number,
        public money: number
    ) {}
}

export default Player;