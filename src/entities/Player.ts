class Player {

    // Substituir NOME por Tell no futuro

    constructor (
        public playerId: string,
        public level: number,
        public xp: number,
        public money: number
    ) {}
}

export default Player;